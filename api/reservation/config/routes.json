{
  "routes": [
    {
      "method": "GET",
      "path": "/reservations",
      "handler": "reservation.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/reservations/count",
      "handler": "reservation.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/reservations/:id",
      "handler": "reservation.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/reservations",
      "handler": "reservation.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/reservations/:id",
      "handler": "reservation.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/reservations/:id",
      "handler": "reservation.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
